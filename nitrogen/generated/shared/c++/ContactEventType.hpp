///
/// ContactEventType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::contactlistner {

  /**
   * An enum which can be represented as a JavaScript union (ContactEventType).
   */
  enum class ContactEventType {
    ADDED      SWIFT_NAME(added) = 0,
    UPDATED      SWIFT_NAME(updated) = 1,
    DELETED      SWIFT_NAME(deleted) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::contactlistner

namespace margelo::nitro {

  using namespace margelo::nitro::contactlistner;

  // C++ ContactEventType <> JS ContactEventType (union)
  template <>
  struct JSIConverter<ContactEventType> final {
    static inline ContactEventType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("added"): return ContactEventType::ADDED;
        case hashString("updated"): return ContactEventType::UPDATED;
        case hashString("deleted"): return ContactEventType::DELETED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ContactEventType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, ContactEventType arg) {
      switch (arg) {
        case ContactEventType::ADDED: return JSIConverter<std::string>::toJSI(runtime, "added");
        case ContactEventType::UPDATED: return JSIConverter<std::string>::toJSI(runtime, "updated");
        case ContactEventType::DELETED: return JSIConverter<std::string>::toJSI(runtime, "deleted");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ContactEventType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("added"):
        case hashString("updated"):
        case hashString("deleted"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
